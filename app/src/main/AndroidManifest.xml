<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pracenjetroskova">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pracenjetroskova">
        <activity android:name=".PocetnoStanje"></activity>
        <activity android:name=".Registracija" />
        <activity android:name=".Prijava">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UrediTrosak" />
        <activity android:name=".UrediStanje" />
        <activity android:name=".UnesiTrosak" />
        <activity android:name=".PovijestTroskova" />
        <activity android:name=".PocetniIzbornik" />
    </application>

</manifest>